---
- hosts: default
  become: True
  vars:
    docker_users:
        - "vagrant"
        
  pre_tasks:
    - name: Set dev environment fact
      set_fact: development=true
    - name: include sapling vars
      include_vars:
        dir: host_vars/sapling

  roles:
    # - { role: tdude.ndppd }
    # - { role: weareinteractive.ufw }
    - { role: manala.apt }
    - { role: azavea.docker }
    - { role: ericsysmin.chrony }
    # - { role: tdude.core }
    - { role: tdude.monitoring }
    # - { role: tdude.minecraft-discord-bridge }
    # - { role: tdude.kerio }
    # - { role: clutterbox.dehydrated }
    - { role: tdude.mediaserver }
    # - { role: tdude.wireguard }
    - { role: tdude.statusfy }
    - { role: tdude.gitlab }
    - { role: tdude.elk }
    # - { role: tdude.matrix }
    - { role: tdude.lolisafe }
    - { role: tdude.traefik }

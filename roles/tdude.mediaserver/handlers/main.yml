---
- name: restart jackett
  docker_service:
    project_src: "{{ mediaserver_installation_folder }}"
    files: docker-compose.yml
    state: present
    restarted: yes
    services:
      - jackett

- name: restart radarr
  docker_service:
    project_src: "{{ mediaserver_installation_folder }}"
    files: docker-compose.yml
    state: present
    restarted: yes
    services:
      - radarr

- name: restart sonarr
  docker_service:
    project_src: "{{ mediaserver_installation_folder }}"
    files: docker-compose.yml
    state: present
    restarted: yes
    services:
      - sonarr

- name: restart deluge
  docker_service:
    project_src: "{{ mediaserver_installation_folder }}"
    files: docker-compose.yml
    state: present
    restarted: yes
    services:
      - deluge

- name: restart plex
  docker_service:
    project_src: "{{ mediaserver_installation_folder }}"
    files: docker-compose.yml
    state: present
    restarted: yes
    services:
      - plex
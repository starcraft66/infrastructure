# L2 "advertisment" that doesn't really do anything
# other than handle automatic address assignment since
# we disabled the speaker.
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: address-assignment
spec:
  ipAddressPools:
  - pool
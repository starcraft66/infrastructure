apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoview-data
  labels:
    app.kubernetes.io/name: photoview
spec:
  storageClassName: freenas-nfs-csi
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Ti

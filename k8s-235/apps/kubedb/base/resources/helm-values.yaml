monitoring:
  enabled: true
  agent: "prometheus.io/operator"
  prometheus:
    namespace: kubedb
  serviceMonitor:
    labels:
      app.kubernetes.io/name: "kubedb-community"

global:
  skipCleaner: true
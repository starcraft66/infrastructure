apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pomf
  labels:
    app.kubernetes.io/name: pomf
spec:
  storageClassName: ""
  accessModes:
  - "ReadWriteMany"
  resources:
    requests:
      storage: "2Ti"
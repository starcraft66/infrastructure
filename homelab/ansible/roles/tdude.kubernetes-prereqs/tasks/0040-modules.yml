---
- name: Add the br_netfilter module
  modprobe:
    name: br_netfilter
    state: present

- name: Load the br_netfilter module on boot
  lineinfile:
    path: /etc/services
    regexp: "^br_netfilter"
    line: "br_netfilter"

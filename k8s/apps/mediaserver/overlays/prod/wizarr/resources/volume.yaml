apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wizarr-config
  labels:
    app.kubernetes.io/name: wizarr
spec:
  storageClassName: freenas-iscsi-csi
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

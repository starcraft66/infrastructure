apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-config
  labels:
    app.kubernetes.io/name: jackett
spec:
  storageClassName: truenas-nfs-spitfire-fast
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
  labels:
    app.kubernetes.io/name: sonarr
spec:
  storageClassName: truenas-iscsi-spitfire-fast
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
